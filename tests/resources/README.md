# 测试资源目录

此目录用于存放测试所需的资源文件。

## 测试文件

- `sample.mp4` - 用于测试 FFmpeg 音频提取功能的视频文件（需手动添加）

## 说明

- 此目录仅用于存放测试 FFmpeg 功能所需的视频文件
- Whisper、翻译模型、LLM 等需要真实模型/API 的测试，请使用 `scripts/debug/` 下的调测脚本
- 单元测试使用 Mock，不需要真实的模型或 API
